{
  "name": "ctdlui",
  "version": "0.0.8",
  "description": "General Purpose React Components",
  "main": "index.js",
  "files": [
    "dist"
  ],
  "scripts": {
    "release:replace": "replace 'ctdlspace.github.com' 'github.com' CHANGELOG.md",
    "release:build": "",
    "release": "npm build && standard-version && git push --follow-tags origin master",
    "dev:reset": "rm -rf dist",
    "dev:prepare": "touch index.css && touch react.js",
    "dev:clean": "rm -rf components && rm index.css && rm react.js",
    "dev:move": "mv dist/components components && mv dist/react.js react.js && mv dist/index.css index.css && rm -rf dist",
    "dev": "echo \"just import as git submodule\" && exit 1",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Rus Inozemtsev",
  "repository": {
    "type": "git",
    "url": "git+git@github.com:ctdlspace/ctdlui.git"
  },
  "license": "MIT",
  "dependencies": {},
  "devDependencies": {
    "@babel/cli": "^7.10.1",
    "@babel/core": "^7.10.1",
    "@babel/preset-env": "^7.10.1",
    "@babel/preset-react": "^7.10.1",
    "broccoli": "^3.4.2",
    "broccoli-babel-transpiler": "^7.4.0",
    "broccoli-cli": "^1.0.0",
    "broccoli-concat": "^4.2.4",
    "broccoli-funnel": "^3.0.3",
    "broccoli-merge-trees": "^4.2.0",
    "nodemon": "^2.0.4",
    "replace": "^1.2.0",
    "standard-version": "^8.0.0"
  },
  "standard-version": {
    "scripts": {
      "postchangelog": "npm run release:replace"
    }
  }
}
